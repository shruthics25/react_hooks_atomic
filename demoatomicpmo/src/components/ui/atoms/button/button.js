import React from "react";
import { Button } from "react-bootstrap";

function Cbutton(props) {
  return <Button variant="outline-primary">{props.buttontnName}</Button>;
}

export default Cbutton;
